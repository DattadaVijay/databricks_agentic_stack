resources:
  model_serving_endpoints:
    hr_agent_endpoint:
      name: hr-ops-agent-endpoint
      config:
        served_entities:
          - entity_name: main.default.hr_ops_agent
            entity_version: "champion"
            scale_to_zero_enabled: true
            workload_size: Small